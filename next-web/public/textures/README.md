# Textures for `next-web`

## Sky

`UniverseCanvas` uses a dark clear color plus **Drei `<Stars />`** layers (no Milky Way texture).

## Planet maps

See [`planets/README.md`](planets/README.md) and `npm run fetch-planet-textures`.
