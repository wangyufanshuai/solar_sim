"use client";
import { parseKerrHighEnergyResponseApiV426, type KerrHighEnergyResponseSummaryV426 } from "./kerrHighEnergyInstrumentResponseV426";
export type KerrHighEnergyResponseSnapshotV426=Readonly<{status:"idle"|"loading"|"ready"|"unavailable";reason:string|null;requestCount:0|1;responseBytes:number;summary:KerrHighEnergyResponseSummaryV426|null}>;const INITIAL=Object.freeze({status:"idle" as const,reason:null,requestCount:0 as const,responseBytes:0,summary:null});const listeners=new Set<()=>void>();let snapshot:KerrHighEnergyResponseSnapshotV426=INITIAL;let requestPromise:Promise<KerrHighEnergyResponseSummaryV426>|null=null;const safe=(value:unknown)=>typeof value==="string"&&/^[a-z0-9-]{1,64}$/.test(value)?value:"request-failed";const publish=(next:KerrHighEnergyResponseSnapshotV426)=>{snapshot=Object.freeze(next);listeners.forEach((listener)=>listener());};
export function loadKerrHighEnergyResponseSummaryV426():Promise<KerrHighEnergyResponseSummaryV426>{if(requestPromise)return requestPromise;publish({status:"loading",reason:null,requestCount:1,responseBytes:0,summary:null});requestPromise=fetch("/api/atlas/relativity-evidence/v426/high-energy-response",{cache:"no-store"}).then(async(response)=>{const declared=Number(response.headers.get("Content-Length"));if(Number.isFinite(declared)&&declared>128*1024)throw new Error("response-too-large");const body=await response.text(),responseBytes=new TextEncoder().encode(body).byteLength;if(responseBytes>128*1024)throw new Error("response-too-large");const result=parseKerrHighEnergyResponseApiV426(JSON.parse(body));if(!response.ok||!result.available||!result.summary){publish({status:"unavailable",reason:safe(result.reason),requestCount:1,responseBytes,summary:null});throw new Error(`v426-response-${safe(result.reason)}`);}publish({status:"ready",reason:null,requestCount:1,responseBytes,summary:result.summary});return result.summary;}).catch((error:unknown)=>{if(snapshot.status!=="unavailable"){const reason=error instanceof Error?error.message.replace(/^v426-response-/,""):null;publish({status:"unavailable",reason:safe(reason),requestCount:1,responseBytes:snapshot.responseBytes,summary:null});}throw error;});return requestPromise;}
export const getKerrHighEnergyResponseSnapshotV426=()=>snapshot;export function subscribeKerrHighEnergyResponseV426(listener:()=>void):()=>void{listeners.add(listener);return()=>listeners.delete(listener);}export function resetKerrHighEnergyResponseClientForTestsV426():void{requestPromise=null;snapshot=INITIAL;listeners.clear();}
