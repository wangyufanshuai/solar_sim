import type { AtlasRelativityEvidenceSnapshotV346 } from "./relativityWorkbenchEvidenceV346";
export type RelativityImageRoiEvidenceV347 = Readonly<{ status: "science-image-index-roi-v347-qualified-browser-pending-dense-incomplete-0-of-49" | "unavailable"; roiArtifactSha256: string | null; replayArtifactSha256: string | null; roiCount: number; maximumSamplesPerRoi: number; summation: string; uncertaintyCombination: string; celestialWcs: "unavailable"; apertureFlux: "unavailable-no-solid-angle-or-celestial-wcs"; deterministic: boolean; intervalConserved: boolean; jsonBytes: number; csvBytes: number; exportBaseline: boolean; noScienceMutation: boolean; denseStatus: "incomplete-0-of-49"; browserQualification: "not-run" }>;
export type AtlasRelativityEvidenceSnapshotV347 = Readonly<{ version: "v347-relativity-evidence-snapshot"; status: "ready" | "corrupt"; base: AtlasRelativityEvidenceSnapshotV346; current: Readonly<{ v347: RelativityImageRoiEvidenceV347 }> }>;
export type RelativityEvidenceResponseV347 = Readonly<{ version: "v347-relativity-evidence-response"; available: boolean; reason: "ready" | "lite-boundary" | "evidence-unavailable" | "evidence-corrupt"; snapshot: AtlasRelativityEvidenceSnapshotV347 | null }>;
export function parseRelativityEvidenceResponseV347(value: unknown): RelativityEvidenceResponseV347 { if (!value || typeof value !== "object" || Array.isArray(value)) throw new Error("v347 evidence response invalid"); const response = value as Partial<RelativityEvidenceResponseV347>; if (response.version !== "v347-relativity-evidence-response" || typeof response.available !== "boolean" || !["ready", "lite-boundary", "evidence-unavailable", "evidence-corrupt"].includes(String(response.reason))) throw new Error("v347 evidence response identity"); if (!response.available) return { version: response.version, available: false, reason: response.reason!, snapshot: null }; if (!response.snapshot || response.snapshot.version !== "v347-relativity-evidence-snapshot" || response.snapshot.status !== "ready") throw new Error("v347 evidence snapshot identity"); return response as RelativityEvidenceResponseV347; }
