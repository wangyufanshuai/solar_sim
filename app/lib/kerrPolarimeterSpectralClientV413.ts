"use client";
import { parseKerrPolarimeterSpectralResponseApiV413, type KerrPolarimeterSpectralSummaryV413 } from "./kerrPolarimeterSpectralResponseV413";
export type KerrPolarimeterSpectralClientSnapshotV413 = Readonly<{ status: "idle" | "loading" | "ready" | "unavailable"; reason: string | null; requestCount: 0 | 1; responseBytes: number; summary: KerrPolarimeterSpectralSummaryV413 | null }>; const INITIAL = Object.freeze({ status: "idle" as const, reason: null, requestCount: 0 as const, responseBytes: 0, summary: null }); const listeners = new Set<() => void>(); let snapshot: KerrPolarimeterSpectralClientSnapshotV413 = INITIAL; let requestPromise: Promise<KerrPolarimeterSpectralSummaryV413> | null = null; const publish = (next: KerrPolarimeterSpectralClientSnapshotV413) => { snapshot = Object.freeze(next); listeners.forEach((listener) => listener()); }, safeReason = (value: unknown) => typeof value === "string" && /^[a-z0-9-]{1,64}$/.test(value) ? value : "request-failed";
export function loadKerrPolarimeterSpectralSummaryV413(): Promise<KerrPolarimeterSpectralSummaryV413> { if (requestPromise) return requestPromise; publish({ status: "loading", reason: null, requestCount: 1, responseBytes: 0, summary: null }); requestPromise = fetch("/api/atlas/relativity-evidence/v413/polarimeter-spectral-response", { cache: "no-store" }).then(async response => { const declared = Number(response.headers.get("Content-Length")); if (Number.isFinite(declared) && declared > 128 * 1024) throw new Error("response-too-large"); const body = await response.text(), responseBytes = new TextEncoder().encode(body).byteLength; if (responseBytes > 128 * 1024) throw new Error("response-too-large"); const result = parseKerrPolarimeterSpectralResponseApiV413(JSON.parse(body)); if (!response.ok || !result.available || !result.summary) { publish({ status: "unavailable", reason: safeReason(result.reason), requestCount: 1, responseBytes, summary: null }); throw new Error(`v413-spectral-${safeReason(result.reason)}`); } publish({ status: "ready", reason: null, requestCount: 1, responseBytes, summary: result.summary }); return result.summary; }).catch((error: unknown) => { if (snapshot.status !== "unavailable") publish({ status: "unavailable", reason: safeReason(error instanceof Error ? error.message.replace(/^v413-spectral-/, "") : null), requestCount: 1, responseBytes: snapshot.responseBytes, summary: null }); throw error; }); return requestPromise; }
export function getKerrPolarimeterSpectralSnapshotV413(): KerrPolarimeterSpectralClientSnapshotV413 { return snapshot; } export function subscribeKerrPolarimeterSpectralV413(listener: () => void): () => void { listeners.add(listener); return () => listeners.delete(listener); } export function resetKerrPolarimeterSpectralClientForTestsV413(): void { requestPromise = null; snapshot = INITIAL; listeners.clear(); }
