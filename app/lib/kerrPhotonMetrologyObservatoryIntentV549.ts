import type { AtlasDeliveryProfile } from "./atlasDeliveryProfile";
import type { KerrPhotonMetrologyDetailSurfaceV548 } from "./kerrPhotonMetrologyObservatoryIntentV548";
export type KerrPhotonMetrologyDetailSurfaceV549 = KerrPhotonMetrologyDetailSurfaceV548 | "measured-sidecar-admission";
export function resolveKerrPhotonMetrologyObservatoryIntentV549(deliveryProfile: AtlasDeliveryProfile, active: boolean) { const authorized = deliveryProfile === "local-shadow" && active; return Object.freeze({ version: "v549-kerr-photon-metrology-observatory-intent-v1" as const, observatoryImportAuthorized: authorized, detailComponentCatalogSize: 51 as const, concurrentDetailSurfaceBudget: (authorized ? 1 : 0) as 0 | 1, sidecarSchemaCount: 2 as const, detectorFieldCount: 13 as const, wcsFieldCount: 23 as const, measuredSidecarCount: 0 as const, mutationRejectionCount: 10 as const, sciencePixelCount: 0 as const, canvasCreated: false as const, sceneRevisionDelta: 0 as const, physicsMutationAllowed: false as const, reason: authorized ? "enabled" : !active ? "inactive" : deliveryProfile === "vercel-lite" ? "lite-boundary" : "standalone-boundary" }); }
