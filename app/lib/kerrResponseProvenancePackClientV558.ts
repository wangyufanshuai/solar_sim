"use client";
import { acquireAtlasResource } from "./atlasResourceLifecycle";
import { parseKerrResponseProvenancePackApiV558, type KerrResponseProvenancePackV558 } from "./kerrResponseProvenancePackV558";
type SnapshotV558 = Readonly<{ status: "idle" | "loading" | "ready" | "unavailable"; reason: string | null; requestCount: 0 | 1; responseBytes: number; summary: KerrResponseProvenancePackV558 | null }>;
const INITIAL: SnapshotV558 = Object.freeze({ status: "idle", reason: null, requestCount: 0, responseBytes: 0, summary: null }); const listeners = new Set<() => void>(); let snapshot = INITIAL; let requestPromise: Promise<KerrResponseProvenancePackV558> | null = null; const publish = (next: SnapshotV558) => { snapshot = Object.freeze(next); listeners.forEach((listener) => listener()); };
export function loadKerrResponseProvenancePackSummaryV558(): Promise<KerrResponseProvenancePackV558> { if (requestPromise) return requestPromise; publish({ status: "loading", reason: null, requestCount: 1, responseBytes: 0, summary: null }); requestPromise = fetch("/api/atlas/relativity-evidence/v558/response-provenance-pack", { cache: "no-store" }).then(async (response) => { const body = await response.text(), responseBytes = new TextEncoder().encode(body).byteLength; if (responseBytes > 128 * 1024) throw new Error("response-too-large"); const api = parseKerrResponseProvenancePackApiV558(JSON.parse(body)); if (!response.ok || !api.available || !api.summary) throw new Error(`v558-response-${api.reason}`); publish({ status: "ready", reason: null, requestCount: 1, responseBytes, summary: api.summary }); return api.summary; }).catch((error: unknown) => { publish({ status: "unavailable", reason: error instanceof Error ? error.message : "request-failed", requestCount: 1, responseBytes: snapshot.responseBytes, summary: null }); throw error; }); return requestPromise; }
export const getKerrResponseProvenancePackSnapshotV558 = () => snapshot; export function subscribeKerrResponseProvenancePackV558(listener: () => void): () => void { listeners.add(listener); const release = acquireAtlasResource("subscription", "relativity-lab", "v558-response-provenance", { owner: "v558-response-provenance", estimatedBytes: 0 }); let done = false; return () => { if (done) return; done = true; listeners.delete(listener); release(); }; } export const getKerrResponseProvenancePackTelemetryV558 = () => Object.freeze({ listenerCount: listeners.size, requestCount: snapshot.requestCount, status: snapshot.status }); export function resetKerrResponseProvenancePackClientForTestsV558(): void { if (listeners.size) throw new Error("v558-response-listener-leak"); snapshot = INITIAL; requestPromise = null; }
