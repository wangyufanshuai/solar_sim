import type { AtlasDeliveryProfile } from "./atlasDeliveryProfile";
import type { KerrPhotonMetrologyDetailSurfaceV546 } from "./kerrPhotonMetrologyObservatoryIntentV546";
export type KerrPhotonMetrologyDetailSurfaceV547 = KerrPhotonMetrologyDetailSurfaceV546 | "blind-authority-review";
export function resolveKerrPhotonMetrologyObservatoryIntentV547(deliveryProfile: AtlasDeliveryProfile, active: boolean) { const authorized = deliveryProfile === "local-shadow" && active; return Object.freeze({ version: "v547-kerr-photon-metrology-observatory-intent-v1" as const, observatoryImportAuthorized: authorized, detailComponentCatalogSize: 49 as const, concurrentDetailSurfaceBudget: (authorized ? 1 : 0) as 0 | 1, reviewerCount: 2 as const, verifiedSignatureCount: 2 as const, rejectedMutationCount: 8 as const, measuredCandidateCount: 0 as const, measuredAuthorityGrantCount: 0 as const, scienceRasterCount: 0 as const, canvasCreated: false as const, sceneRevisionDelta: 0 as const, physicsMutationAllowed: false as const, reason: authorized ? "enabled" : !active ? "inactive" : deliveryProfile === "vercel-lite" ? "lite-boundary" : "standalone-boundary" }); }
