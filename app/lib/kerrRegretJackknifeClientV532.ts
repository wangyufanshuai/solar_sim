"use client";import{acquireAtlasResource}from"./atlasResourceLifecycle";import{parseKerrRegretJackknifeApiV532,type KerrRegretJackknifeSummaryV532}from"./kerrRegretJackknifeV532";export const KERR_REGRET_JACKKNIFE_SUMMARY_MAX_BYTES_V532=128*1024;type Snapshot=Readonly<{status:"idle"|"loading"|"ready"|"unavailable";reason:string|null;requestCount:0|1;responseBytes:number;summary:KerrRegretJackknifeSummaryV532|null}>;const INITIAL:Snapshot=Object.freeze({status:"idle",reason:null,requestCount:0,responseBytes:0,summary:null}),listeners=new Set<()=>void>();let snapshot:Snapshot=INITIAL,requestPromise:Promise<KerrRegretJackknifeSummaryV532>|null=null;const publish=(next:Snapshot)=>{snapshot=Object.freeze(next);listeners.forEach((listener)=>listener());};export function loadKerrRegretJackknifeSummaryV532(){if(requestPromise)return requestPromise;publish({status:"loading",reason:null,requestCount:1,responseBytes:0,summary:null});requestPromise=fetch("/api/atlas/relativity-evidence/v532/regret-jackknife",{cache:"no-store"}).then(async(response)=>{const declared=Number(response.headers.get("Content-Length"));if(Number.isFinite(declared)&&declared>KERR_REGRET_JACKKNIFE_SUMMARY_MAX_BYTES_V532)throw new Error("response-too-large");const body=await response.text(),responseBytes=new TextEncoder().encode(body).byteLength;if(responseBytes>KERR_REGRET_JACKKNIFE_SUMMARY_MAX_BYTES_V532)throw new Error("response-too-large");const api=parseKerrRegretJackknifeApiV532(JSON.parse(body));if(!response.ok||!api.available||!api.summary)throw new Error(`v532-jackknife-${api.reason}`);publish({status:"ready",reason:null,requestCount:1,responseBytes,summary:api.summary});return api.summary;}).catch((error:unknown)=>{publish({status:"unavailable",reason:error instanceof Error?error.message:"request-failed",requestCount:1,responseBytes:snapshot.responseBytes,summary:null});throw error;});return requestPromise;}export const getKerrRegretJackknifeSnapshotV532=()=>snapshot;export function subscribeKerrRegretJackknifeV532(listener:()=>void){listeners.add(listener);const release=acquireAtlasResource("subscription","relativity-lab","v532-jackknife",{owner:"v532-regret-jackknife",estimatedBytes:0});let done=false;return()=>{if(done)return;done=true;listeners.delete(listener);release();};}export function getKerrRegretJackknifeTelemetryV532(){return Object.freeze({listenerCount:listeners.size,requestCount:snapshot.requestCount,status:snapshot.status,sceneRevisionDelta:0 as const,scientificFieldMutationAllowed:false as const});}export function resetKerrRegretJackknifeClientForTestsV532(){if(listeners.size)throw new Error("v532-listener-leak");snapshot=INITIAL;requestPromise=null;}
