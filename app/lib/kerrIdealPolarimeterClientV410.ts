"use client";
import { parseKerrIdealPolarimeterResponseV410, type KerrIdealPolarimeterSummaryV410 } from "./kerrIdealPolarimeterV410";
export type KerrIdealPolarimeterClientSnapshotV410 = Readonly<{ status: "idle" | "loading" | "ready" | "unavailable"; reason: string | null; requestCount: 0 | 1; responseBytes: number; summary: KerrIdealPolarimeterSummaryV410 | null }>;
const INITIAL = Object.freeze({ status: "idle" as const, reason: null, requestCount: 0 as const, responseBytes: 0, summary: null }); const listeners = new Set<() => void>(); let snapshot: KerrIdealPolarimeterClientSnapshotV410 = INITIAL; let requestPromise: Promise<KerrIdealPolarimeterSummaryV410> | null = null; const publish = (next: KerrIdealPolarimeterClientSnapshotV410) => { snapshot = Object.freeze(next); listeners.forEach((listener) => listener()); }; const safeReason = (value: unknown) => typeof value === "string" && /^[a-z0-9-]{1,64}$/.test(value) ? value : "request-failed";
export function loadKerrIdealPolarimeterSummaryV410(): Promise<KerrIdealPolarimeterSummaryV410> { if (requestPromise) return requestPromise; publish({ status: "loading", reason: null, requestCount: 1, responseBytes: 0, summary: null }); requestPromise = fetch("/api/atlas/relativity-evidence/v410/ideal-polarimeter", { cache: "no-store" }).then(async (response) => { const declared = Number(response.headers.get("Content-Length")); if (Number.isFinite(declared) && declared > 128 * 1024) throw new Error("response-too-large"); const body = await response.text(); const responseBytes = new TextEncoder().encode(body).byteLength; if (responseBytes > 128 * 1024) throw new Error("response-too-large"); const result = parseKerrIdealPolarimeterResponseV410(JSON.parse(body)); if (!response.ok || !result.available || !result.summary) { publish({ status: "unavailable", reason: safeReason(result.reason), requestCount: 1, responseBytes, summary: null }); throw new Error(`v410-polarimeter-${safeReason(result.reason)}`); } publish({ status: "ready", reason: null, requestCount: 1, responseBytes, summary: result.summary }); return result.summary; }).catch((error: unknown) => { if (snapshot.status !== "unavailable") publish({ status: "unavailable", reason: safeReason(error instanceof Error ? error.message.replace(/^v410-polarimeter-/, "") : null), requestCount: 1, responseBytes: snapshot.responseBytes, summary: null }); throw error; }); return requestPromise; }
export function getKerrIdealPolarimeterSnapshotV410(): KerrIdealPolarimeterClientSnapshotV410 { return snapshot; } export function subscribeKerrIdealPolarimeterV410(listener: () => void): () => void { listeners.add(listener); return () => listeners.delete(listener); } export function resetKerrIdealPolarimeterClientForTestsV410(): void { requestPromise = null; snapshot = INITIAL; listeners.clear(); }
