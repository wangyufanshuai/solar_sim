import type { AtlasDeliveryProfile } from "./atlasDeliveryProfile";
import type { KerrPhotonMetrologyDetailSurfaceV555 } from "./kerrPhotonMetrologyObservatoryIntentV555";
export type KerrPhotonMetrologyDetailSurfaceV556 = KerrPhotonMetrologyDetailSurfaceV555 | "sparse-science-products";
export function resolveKerrPhotonMetrologyObservatoryIntentV556(deliveryProfile: AtlasDeliveryProfile, active: boolean) { const authorized = deliveryProfile === "local-shadow" && active; return Object.freeze({ version: "v556-kerr-photon-metrology-observatory-intent-v1" as const, observatoryImportAuthorized: authorized, detailComponentCatalogSize: 57 as const, concurrentDetailSurfaceBudget: (authorized ? 1 : 0) as 0 | 1, sparseScienceSampleCount: 16 as const, sparseProductCount: 2 as const, exactPixelReadbackValidatorCount: 2 as const, observedFrameCount: 0 as const, measuredPixelCount: 0 as const, networkAttemptCount: 0 as const, canvasCreated: false as const, sceneRevisionDelta: 0 as const, physicsMutationAllowed: false as const, reason: authorized ? "enabled" : !active ? "inactive" : deliveryProfile === "vercel-lite" ? "lite-boundary" : "standalone-boundary" }); }
