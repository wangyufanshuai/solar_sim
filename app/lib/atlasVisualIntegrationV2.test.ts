import{describe,expect,it}from"vitest";import{createAtlasVisualIntegrationV2Summary}from"./atlasVisualIntegrationV2";describe("v124 visual integration",()=>{it("locks review and performance gates",()=>{const s=createAtlasVisualIntegrationV2Summary();expect(s.reviewScenes).toContain("trappist-1");expect(s.reviewScenes).toContain("payload-deploy");expect(s.search.coldShardMaxMs).toBe(250);expect(s.stellarCloseupDrawCallsMax).toBe(20)});});
