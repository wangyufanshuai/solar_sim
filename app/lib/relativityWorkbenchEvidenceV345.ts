import type { AtlasRelativityEvidenceSnapshotV344 } from "./relativityWorkbenchEvidenceV344";
export type RelativityPreviewRuntimeEvidenceV345 = Readonly<{ status: "science-image-preview-runtime-v345-qualified-browser-pending-dense-incomplete-0-of-49" | "unavailable"; sequence: readonly string[]; sceneRevisionDelta: number; canvasCount: number; selectionStable: boolean; cameraLeaseBaseline: boolean; duringObjectUrls: number; duringTypedArrayCaches: number; afterPngBaseline: boolean; afterFitsBaseline: boolean; finalBaseline: boolean; portraitAccepted: boolean; landscapeAccepted: boolean; noObjectUrlDuplicationOnRotation: boolean; noScienceMutation: boolean; telemetryBridge: string; denseStatus: "incomplete-0-of-49"; browserQualification: "not-run"; artifactSha256: string | null }>;
export type AtlasRelativityEvidenceSnapshotV345 = Readonly<{ version: "v345-relativity-evidence-snapshot"; status: "ready" | "corrupt"; base: AtlasRelativityEvidenceSnapshotV344; current: Readonly<{ v345: RelativityPreviewRuntimeEvidenceV345 }> }>;
export type RelativityEvidenceResponseV345 = Readonly<{ version: "v345-relativity-evidence-response"; available: boolean; reason: "ready" | "lite-boundary" | "evidence-unavailable" | "evidence-corrupt"; snapshot: AtlasRelativityEvidenceSnapshotV345 | null }>;
export function parseRelativityEvidenceResponseV345(value: unknown): RelativityEvidenceResponseV345 { if (!value || typeof value !== "object" || Array.isArray(value)) throw new Error("v345 evidence response invalid"); const response = value as Partial<RelativityEvidenceResponseV345>; if (response.version !== "v345-relativity-evidence-response" || typeof response.available !== "boolean" || !["ready", "lite-boundary", "evidence-unavailable", "evidence-corrupt"].includes(String(response.reason))) throw new Error("v345 evidence response identity"); if (!response.available) return { version: response.version, available: false, reason: response.reason!, snapshot: null }; if (!response.snapshot || response.snapshot.version !== "v345-relativity-evidence-snapshot" || response.snapshot.status !== "ready") throw new Error("v345 evidence snapshot identity"); return response as RelativityEvidenceResponseV345; }
