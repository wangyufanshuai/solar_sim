import type { AtlasDeliveryProfile } from "./atlasDeliveryProfile";
import type { KerrPhotonMetrologyDetailSurfaceV547 } from "./kerrPhotonMetrologyObservatoryIntentV547";
export type KerrPhotonMetrologyDetailSurfaceV548 = KerrPhotonMetrologyDetailSurfaceV547 | "observation-quantity-provenance";
export function resolveKerrPhotonMetrologyObservatoryIntentV548(deliveryProfile: AtlasDeliveryProfile, active: boolean) { const authorized = deliveryProfile === "local-shadow" && active; return Object.freeze({ version: "v548-kerr-photon-metrology-observatory-intent-v1" as const, observatoryImportAuthorized: authorized, detailComponentCatalogSize: 50 as const, concurrentDetailSurfaceBudget: (authorized ? 1 : 0) as 0 | 1, observationQuantityCount: 10 as const, qualifiedShortAuthorityQuantityCount: 5 as const, missingMeasuredFileCount: 6 as const, measuredAuthorityGrantCount: 0 as const, scienceRasterCount: 0 as const, canvasCreated: false as const, sceneRevisionDelta: 0 as const, physicsMutationAllowed: false as const, reason: authorized ? "enabled" : !active ? "inactive" : deliveryProfile === "vercel-lite" ? "lite-boundary" : "standalone-boundary" }); }
