import type { AtlasDeliveryProfile } from "./atlasDeliveryProfile";
import type { KerrPhotonMetrologyDetailSurfaceV542 } from "./kerrPhotonMetrologyObservatoryIntentV542";
export type KerrPhotonMetrologyDetailSurfaceV543 = KerrPhotonMetrologyDetailSurfaceV542 | "isolated-replay-preflight";
export function resolveKerrPhotonMetrologyObservatoryIntentV543(deliveryProfile: AtlasDeliveryProfile, active: boolean) { const authorized = deliveryProfile === "local-shadow" && active; return Object.freeze({ version: "v543-kerr-photon-metrology-observatory-intent-v1" as const, observatoryImportAuthorized: authorized, detailComponentCatalogSize: 45 as const, concurrentDetailSurfaceBudget: (authorized ? 1 : 0) as 0 | 1, executionCellCount: 5 as const, executedEnvironmentCount: 2 as const, scienceQualifiedEnvironmentCount: 1 as const, isolatedSubprocessCount: 2 as const, scienceDependencyImportCount: 0 as const, crossEnvironmentScienceQualified: false as const, fitsImageHduCount: 0 as const, canvasCreated: false as const, sceneRevisionDelta: 0 as const, physicsMutationAllowed: false as const, reason: authorized ? "enabled" : !active ? "inactive" : deliveryProfile === "vercel-lite" ? "lite-boundary" : "standalone-boundary" }); }
