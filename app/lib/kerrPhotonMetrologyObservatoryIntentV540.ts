import type { AtlasDeliveryProfile } from "./atlasDeliveryProfile";
import type { KerrPhotonMetrologyDetailSurfaceV539 } from "./kerrPhotonMetrologyObservatoryIntentV539";

export type KerrPhotonMetrologyDetailSurfaceV540 = KerrPhotonMetrologyDetailSurfaceV539 | "response-aware-offline-replay";
export function resolveKerrPhotonMetrologyObservatoryIntentV540(deliveryProfile: AtlasDeliveryProfile, active: boolean) { const authorized = deliveryProfile === "local-shadow" && active; return Object.freeze({ version: "v540-kerr-photon-metrology-observatory-intent-v1" as const, observatoryImportAuthorized: authorized, detailComponentCatalogSize: 42 as const, concurrentDetailSurfaceBudget: (authorized ? 1 : 0) as 0 | 1, replayAttemptCount: 2 as const, networkAttemptCount: 0 as const, temporaryDirectoryLeakCount: 0 as const, fitsImageHduCount: 0 as const, canvasCreated: false as const, sceneRevisionDelta: 0 as const, physicsMutationAllowed: false as const, reason: authorized ? "enabled" : !active ? "inactive" : deliveryProfile === "vercel-lite" ? "lite-boundary" : "standalone-boundary" }); }
