import type { AtlasDeliveryProfile } from "./atlasDeliveryProfile";
import type { KerrPhotonMetrologyDetailSurfaceV550 } from "./kerrPhotonMetrologyObservatoryIntentV550";
export type KerrPhotonMetrologyDetailSurfaceV551 = KerrPhotonMetrologyDetailSurfaceV550 | "license-confirmation";
export function resolveKerrPhotonMetrologyObservatoryIntentV551(deliveryProfile: AtlasDeliveryProfile, active: boolean) { const authorized = deliveryProfile === "local-shadow" && active; return Object.freeze({ version: "v551-kerr-photon-metrology-observatory-intent-v1" as const, observatoryImportAuthorized: authorized, detailComponentCatalogSize: 53 as const, concurrentDetailSurfaceBudget: (authorized ? 1 : 0) as 0 | 1, confirmationMemberCount: 7 as const, confirmedCount: 0 as const, unconfirmedCount: 7 as const, replayAttemptCount: 2 as const, networkAttemptCount: 0 as const, measuredAuthorityGrantCount: 0 as const, sciencePixelCount: 0 as const, canvasCreated: false as const, sceneRevisionDelta: 0 as const, physicsMutationAllowed: false as const, reason: authorized ? "enabled" : !active ? "inactive" : deliveryProfile === "vercel-lite" ? "lite-boundary" : "standalone-boundary" }); }
