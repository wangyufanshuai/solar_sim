"use client";
import { parseKerrRedshiftMinimumCutApiV497, type KerrRedshiftMinimumCutSummaryV497 } from "./kerrRedshiftMinimumCutV497";
export type KerrRedshiftMinimumCutSnapshotV497 = Readonly<{ status: "idle" | "loading" | "ready" | "unavailable"; reason: string | null; requestCount: 0 | 1; responseBytes: number; summary: KerrRedshiftMinimumCutSummaryV497 | null }>;
const INITIAL: KerrRedshiftMinimumCutSnapshotV497 = Object.freeze({ status: "idle", reason: null, requestCount: 0, responseBytes: 0, summary: null }), listeners = new Set<() => void>(); let snapshot = INITIAL, requestPromise: Promise<KerrRedshiftMinimumCutSummaryV497> | null = null; const safeReason = (value: unknown) => typeof value === "string" && /^[a-z0-9-]{1,64}$/.test(value) ? value : "request-failed", publish = (next: KerrRedshiftMinimumCutSnapshotV497) => { snapshot = Object.freeze(next); listeners.forEach((listener) => listener()); };
export function loadKerrRedshiftMinimumCutSummaryV497(): Promise<KerrRedshiftMinimumCutSummaryV497> { if (requestPromise) return requestPromise; publish({ status: "loading", reason: null, requestCount: 1, responseBytes: 0, summary: null }); requestPromise = fetch("/api/atlas/relativity-evidence/v497/redshift-minimum-cut", { cache: "no-store" }).then(async (response) => { const declared = Number(response.headers.get("Content-Length")); if (Number.isFinite(declared) && declared > 128 * 1024) throw new Error("response-too-large"); const body = await response.text(), responseBytes = new TextEncoder().encode(body).byteLength; if (responseBytes > 128 * 1024) throw new Error("response-too-large"); const api = parseKerrRedshiftMinimumCutApiV497(JSON.parse(body)); if (!response.ok || !api.available || !api.summary) { const reason = safeReason(api.reason); publish({ status: "unavailable", reason, requestCount: 1, responseBytes, summary: null }); throw new Error(`v497-response-${reason}`); } publish({ status: "ready", reason: null, requestCount: 1, responseBytes, summary: api.summary }); return api.summary; }).catch((error: unknown) => { if (snapshot.status !== "unavailable") publish({ status: "unavailable", reason: safeReason(error instanceof Error ? error.message.replace(/^v497-response-/i, "") : null), requestCount: 1, responseBytes: snapshot.responseBytes, summary: null }); throw error; }); return requestPromise; }
export const getKerrRedshiftMinimumCutSnapshotV497 = () => snapshot; export function subscribeKerrRedshiftMinimumCutV497(listener: () => void): () => void { listeners.add(listener); return () => listeners.delete(listener); } export function resetKerrRedshiftMinimumCutClientForTestsV497(): void { requestPromise = null; snapshot = INITIAL; listeners.clear(); }
