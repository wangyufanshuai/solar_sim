import type { AtlasRelativityEvidenceSnapshotV347 } from "./relativityWorkbenchEvidenceV347";
export type RelativityBandContrastEvidenceV348 = Readonly<{ status: "science-band-contrast-v348-qualified-browser-pending-dense-incomplete-0-of-49" | "unavailable"; diagnosticArtifactSha256: string | null; replayArtifactSha256: string | null; rowCount: number; rayCount: number; ratiosPerRay: number; maximumRelativeEnvelope: number; maximumInstrumentQuadrature: number; detectorAndSyntheticSeparated: boolean; spectralTemperature: "unavailable"; calibratedHardness: "unavailable"; noCinematicInput: boolean; singleFlight: boolean; cacheBaseline: boolean; exportBaseline: boolean; jsonBytes: number; csvBytes: number; denseStatus: "incomplete-0-of-49"; browserQualification: "not-run" }>;
export type AtlasRelativityEvidenceSnapshotV348 = Readonly<{ version: "v348-relativity-evidence-snapshot"; status: "ready" | "corrupt"; base: AtlasRelativityEvidenceSnapshotV347; current: Readonly<{ v348: RelativityBandContrastEvidenceV348 }> }>;
export type RelativityEvidenceResponseV348 = Readonly<{ version: "v348-relativity-evidence-response"; available: boolean; reason: "ready" | "lite-boundary" | "evidence-unavailable" | "evidence-corrupt"; snapshot: AtlasRelativityEvidenceSnapshotV348 | null }>;
export function parseRelativityEvidenceResponseV348(value: unknown): RelativityEvidenceResponseV348 { if (!value || typeof value !== "object" || Array.isArray(value)) throw new Error("v348 evidence response invalid"); const response = value as Partial<RelativityEvidenceResponseV348>; if (response.version !== "v348-relativity-evidence-response" || typeof response.available !== "boolean" || !["ready", "lite-boundary", "evidence-unavailable", "evidence-corrupt"].includes(String(response.reason))) throw new Error("v348 evidence response identity"); if (!response.available) return { version: response.version, available: false, reason: response.reason!, snapshot: null }; if (!response.snapshot || response.snapshot.version !== "v348-relativity-evidence-snapshot" || response.snapshot.status !== "ready") throw new Error("v348 evidence snapshot identity"); return response as RelativityEvidenceResponseV348; }
