"use client";
import { acquireAtlasResource } from "./atlasResourceLifecycle"; import { serializeKerrScienceBandContrastCsvV348, serializeKerrScienceBandContrastJsonV348, type KerrScienceBandContrastRowV348 } from "./kerrScienceBandContrastV348";
export type KerrScienceBandContrastExportFormatV348 = "json" | "csv"; export type AcquiredKerrScienceBandContrastExportV348 = Readonly<{ format: KerrScienceBandContrastExportFormatV348; filename: string; objectUrl: string; bytes: number; release: () => void }>;
export function acquireKerrScienceBandContrastExportV348(rows: readonly KerrScienceBandContrastRowV348[], artifactSha256: string, format: KerrScienceBandContrastExportFormatV348): AcquiredKerrScienceBandContrastExportV348 { const content = format === "json" ? serializeKerrScienceBandContrastJsonV348(rows, artifactSha256) : serializeKerrScienceBandContrastCsvV348(rows, artifactSha256); const blob = new Blob([content], { type: format === "json" ? "application/json" : "text/csv;charset=utf-8" }); const objectUrl = URL.createObjectURL(blob); const releaseRegistry = acquireAtlasResource("object-url", "relativity-lab", `v348:band-contrast-export:${format}`, { owner: "v348-band-contrast", estimatedBytes: blob.size, contentSha256: artifactSha256, manifestSha256: artifactSha256 }); let released = false; return Object.freeze({ format, filename: `orbit-atlas-ray-${rows[0].rayIndex}-band-contrast-v348.${format}`, objectUrl, bytes: blob.size, release: () => { if (released) return; released = true; URL.revokeObjectURL(objectUrl); releaseRegistry(); } }); }
