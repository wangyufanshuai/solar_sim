import type { AtlasDeliveryProfile } from "./atlasDeliveryProfile";
import type { KerrPhotonMetrologyDetailSurfaceV553 } from "./kerrPhotonMetrologyObservatoryIntentV553";
export type KerrPhotonMetrologyDetailSurfaceV555 = KerrPhotonMetrologyDetailSurfaceV553 | "sparse-science-image";
export function resolveKerrPhotonMetrologyObservatoryIntentV555(deliveryProfile: AtlasDeliveryProfile, active: boolean) { const authorized = deliveryProfile === "local-shadow" && active; return Object.freeze({ version: "v555-kerr-photon-metrology-observatory-intent-v1" as const, observatoryImportAuthorized: authorized, detailComponentCatalogSize: 56 as const, concurrentDetailSurfaceBudget: (authorized ? 1 : 0) as 0 | 1, sparseScienceSampleCount: 16 as const, sparseDisplayPixelCount: 514 as const, observedFrameCount: 0 as const, measuredPixelCount: 0 as const, networkAttemptCount: 0 as const, canvasCreated: false as const, sceneRevisionDelta: 0 as const, physicsMutationAllowed: false as const, reason: authorized ? "enabled" : !active ? "inactive" : deliveryProfile === "vercel-lite" ? "lite-boundary" : "standalone-boundary" }); }
