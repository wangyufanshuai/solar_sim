import type { AtlasRelativityEvidenceSnapshotV345 } from "./relativityWorkbenchEvidenceV345";
export type RelativityImageProbeEvidenceV346 = Readonly<{ status: "science-image-index-probe-v346-qualified-browser-pending-dense-incomplete-0-of-49" | "unavailable"; probeArtifactSha256: string | null; replayArtifactSha256: string | null; hitCount: number; uniqueHitCount: number; orientationInvariant: boolean; singleFlightRequests: number; typedArrayCacheDelta: number; releaseBaseline: boolean; corruptBaseline: boolean; celestialWcs: "unavailable"; noScienceMutation: boolean; sceneRevisionDelta: number; canvasCount: number; selectionStable: boolean; denseStatus: "incomplete-0-of-49"; browserQualification: "not-run" }>;
export type AtlasRelativityEvidenceSnapshotV346 = Readonly<{ version: "v346-relativity-evidence-snapshot"; status: "ready" | "corrupt"; base: AtlasRelativityEvidenceSnapshotV345; current: Readonly<{ v346: RelativityImageProbeEvidenceV346 }> }>;
export type RelativityEvidenceResponseV346 = Readonly<{ version: "v346-relativity-evidence-response"; available: boolean; reason: "ready" | "lite-boundary" | "evidence-unavailable" | "evidence-corrupt"; snapshot: AtlasRelativityEvidenceSnapshotV346 | null }>;
export function parseRelativityEvidenceResponseV346(value: unknown): RelativityEvidenceResponseV346 { if (!value || typeof value !== "object" || Array.isArray(value)) throw new Error("v346 evidence response invalid"); const response = value as Partial<RelativityEvidenceResponseV346>; if (response.version !== "v346-relativity-evidence-response" || typeof response.available !== "boolean" || !["ready", "lite-boundary", "evidence-unavailable", "evidence-corrupt"].includes(String(response.reason))) throw new Error("v346 evidence response identity"); if (!response.available) return { version: response.version, available: false, reason: response.reason!, snapshot: null }; if (!response.snapshot || response.snapshot.version !== "v346-relativity-evidence-snapshot" || response.snapshot.status !== "ready") throw new Error("v346 evidence snapshot identity"); return response as RelativityEvidenceResponseV346; }
