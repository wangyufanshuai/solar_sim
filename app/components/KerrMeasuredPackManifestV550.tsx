"use client";
import { useEffect, useState, useSyncExternalStore, type CSSProperties } from "react";
import { getKerrMeasuredPackManifestSnapshotV550, loadKerrMeasuredPackManifestSummaryV550, subscribeKerrMeasuredPackManifestV550 } from "../lib/kerrMeasuredPackManifestClientV550";
import { createKerrMeasuredPackHudEncodingV550, resolveKerrMeasuredPackHudProfileV550, type KerrMeasuredPackHudModeV550 } from "../lib/kerrMeasuredPackManifestV550";
export default function KerrMeasuredPackManifestV550() { const [mode, setMode] = useState<KerrMeasuredPackHudModeV550>("science"), state = useSyncExternalStore(subscribeKerrMeasuredPackManifestV550, getKerrMeasuredPackManifestSnapshotV550, getKerrMeasuredPackManifestSnapshotV550), artifact = state.summary, profile = resolveKerrMeasuredPackHudProfileV550(mode), encoding = artifact ? createKerrMeasuredPackHudEncodingV550(artifact, mode) : null; useEffect(() => { void loadKerrMeasuredPackManifestSummaryV550().catch(() => undefined); }, []); const style = { "--v550-panel": profile.panel, "--v550-raised": profile.panelRaised, "--v550-ink": profile.ink, "--v550-muted": profile.muted, "--v550-qualified": profile.qualified, "--v550-warning": profile.warning, "--v550-unavailable": profile.unavailable } as CSSProperties; return <section style={style} className="relative mt-7 overflow-hidden rounded-[48px] border border-white/10 bg-[var(--v550-panel)] p-6 font-mono text-[var(--v550-ink)]" data-atlas-measured-pack-manifest-v550 data-atlas-v550-mode={mode} data-atlas-v550-members={encoding?.memberIds.length ?? 0} data-atlas-v550-measured-authority="false" data-atlas-v550-science-raster-qualified="false" data-atlas-v550-canvas-created="false" data-atlas-v550-scene-revision-delta="0"><div className="pointer-events-none absolute inset-0 opacity-45 [background-image:radial-gradient(circle_at_13%_8%,rgba(124,238,195,.18),transparent_24%),radial-gradient(circle_at_89%_18%,rgba(100,199,255,.14),transparent_28%),linear-gradient(rgba(110,210,255,.04)_1px,transparent_1px),linear-gradient(90deg,rgba(110,210,255,.04)_1px,transparent_1px)] [background-size:auto,auto,40px_40px]" /><header className="relative border-b border-white/10 pb-6"><div className="flex flex-wrap items-start justify-between gap-4"><div><div className="text-[9px] uppercase tracking-[.44em] text-[var(--v550-muted)]">V550 / Science Cinematic V9.9</div><h2 className="mt-4 max-w-4xl font-serif text-4xl">六类 measured 输入必须先有固定 manifest</h2></div><div className="flex border border-white/10">{(["science", "cinematic"] as const).map((item) => <button key={item} type="button" aria-pressed={mode === item} className={mode === item ? "bg-white/10 px-3 py-2 text-[8px] text-white" : "px-3 py-2 text-[8px] text-[var(--v550-muted)]"} onClick={() => setMode(item)}>{item}</button>)}</div></div><p className="mt-4 max-w-4xl text-[10px] leading-6 text-[var(--v550-muted)]">manifest 固定六类 measured 成员加一个 provenance sidecar，保存来源 URL 和 license snapshot；当前所有成员均缺失。两次离线 replay 仅验证成员顺序、缺失 fail-closed、路径净化和 canonical SHA，不把空 manifest 当作科学数据。</p></header>{!artifact ? <div className="relative mt-6 text-[10px] text-[var(--v550-muted)]">{state.status === "unavailable" ? `measured pack manifest 不可用 / ${state.reason ?? "request-failed"}` : "正在读取 pack manifest…"}</div> : <><div className="relative mt-6 grid gap-2 sm:grid-cols-3 xl:grid-cols-6"><Metric label="members" value="0 / 7" unavailable /><Metric label="measured" value="0 / 6" unavailable /><Metric label="provenance" value="0 / 1" unavailable /><Metric label="licenses" value="0 / 7 confirmed" unavailable /><Metric label="offline replay" value="2 / 2" /><Metric label="mutations rejected" value="8 / 8" /></div><div className="relative mt-5 grid gap-4 xl:grid-cols-[1.25fr_.75fr]"><div className="grid gap-2 sm:grid-cols-2">{artifact.members.map((entry) => <article key={entry.id} className="rounded-2xl border border-white/10 bg-[var(--v550-raised)] p-4"><div className="flex items-center justify-between gap-3"><span className="text-[9px] text-white">{entry.fileName}</span><span className="text-[8px] text-[var(--v550-unavailable)]">missing</span></div><div className="mt-3 grid gap-1"><Row label="role" value={entry.role} /><Row label="format" value={entry.format} /><Row label="license" value="fixture terms only" /><Row label="bytes / SHA" value="null / null" /></div></article>)}</div><aside className="rounded-2xl border border-white/10 bg-black/20 p-5"><div className="text-[8px] uppercase tracking-[.22em] text-[var(--v550-muted)]">Offline replay boundary</div><div className="mt-4 grid gap-2"><Row label="attempt A / B" value="canonical identical" /><Row label="network" value="0" /><Row label="absolute paths" value="0" /><Row label="PID/host fields" value="0" /><Row label="pack root" value="absent" /><Row label="measured authority" value="false" /></div><div className="mt-5 border border-[var(--v550-warning)]/20 bg-[var(--v550-warning)]/[.04] p-3 text-[8px] leading-5 text-[var(--v550-warning)]">license snapshot 是 synthetic terms-only；真实许可证、measured 字节和 provenance sidecar 到达并通过 replay 前，不能进入 science raster 或 production package。</div></aside></div><footer className="relative mt-5 flex flex-wrap items-center justify-between gap-3 border-t border-white/10 pt-4 text-[8px] text-[var(--v550-muted)]"><span>empty pack · pixels 0 · dense 0/49 · browser qualification not-run</span><div className="flex flex-wrap gap-2"><Download id="receipt" label="replay 回执" /><Download id="atlas" label="manifest atlas" /></div></footer></>}</section>; }
function Metric({ label, value, unavailable = false }: { label: string; value: string; unavailable?: boolean }) { return <div className="border border-white/10 bg-black/20 p-3"><div className="text-[8px] uppercase tracking-[.16em] text-[var(--v550-muted)]">{label}</div><div className={unavailable ? "mt-2 text-base text-[var(--v550-unavailable)]" : "mt-2 text-lg text-[var(--v550-qualified)]"}>{value}</div></div>; }
function Row({ label, value }: { label: string; value: string }) { return <div className="flex justify-between gap-3 border-b border-white/5 py-2 text-[8px]"><span className="text-[var(--v550-muted)]">{label}</span><span className="text-right">{value}</span></div>; }
function Download({ id, label }: { id: "atlas" | "receipt"; label: string }) { return <a className="border border-white/15 px-3 py-2 text-white/70" href={`/api/atlas/relativity-evidence/v550/measured-pack-manifest?download=${id}`}>{label}</a>; }
