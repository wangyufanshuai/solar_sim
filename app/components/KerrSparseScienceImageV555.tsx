"use client";
import { useEffect, useState, useSyncExternalStore, type CSSProperties } from "react";
import { getKerrSparseScienceImageSnapshotV555, loadKerrSparseScienceImageSummaryV555, subscribeKerrSparseScienceImageV555 } from "../lib/kerrSparseScienceImageClientV555";
import { createKerrSparseScienceImageHudEncodingV555, resolveKerrSparseScienceImageHudProfileV555, type KerrSparseScienceImageHudModeV555 } from "../lib/kerrSparseScienceImageV555";
export default function KerrSparseScienceImageV555() { const [mode, setMode] = useState<KerrSparseScienceImageHudModeV555>("science"), state = useSyncExternalStore(subscribeKerrSparseScienceImageV555, getKerrSparseScienceImageSnapshotV555, getKerrSparseScienceImageSnapshotV555), artifact = state.summary, profile = resolveKerrSparseScienceImageHudProfileV555(mode), encoding = artifact ? createKerrSparseScienceImageHudEncodingV555(artifact, mode) : null; useEffect(() => { void loadKerrSparseScienceImageSummaryV555().catch(() => undefined); }, []); const style = { "--v555-panel": profile.panel, "--v555-raised": profile.panelRaised, "--v555-ink": profile.ink, "--v555-muted": profile.muted, "--v555-qualified": profile.qualified, "--v555-warning": profile.warning, "--v555-unavailable": profile.unavailable } as CSSProperties; return <section style={style} className="relative mt-7 overflow-hidden rounded-[36px] border border-white/10 bg-[var(--v555-panel)] p-6 font-mono text-[var(--v555-ink)]" data-atlas-sparse-science-image-v555 data-atlas-v555-mode={mode} data-atlas-v555-samples={encoding?.sampleCount ?? 0} data-atlas-v555-dense-qualified="false" data-atlas-v555-observed-qualified="false" data-atlas-v555-canvas-created="false" data-atlas-v555-scene-revision-delta="0"><header className="border-b border-white/10 pb-5"><div className="flex flex-wrap items-start justify-between gap-4"><div><div className="text-[9px] uppercase tracking-[.4em] text-[var(--v555-muted)]">V555 / Sparse Science Image</div><h2 className="mt-3 max-w-4xl font-serif text-3xl">十六条权威射线，不冒充完整科学影像</h2></div><div className="flex border border-white/10">{(["science", "cinematic"] as const).map((item) => <button key={item} type="button" aria-pressed={mode === item} className={mode === item ? "bg-white/10 px-3 py-2 text-[8px] text-white" : "px-3 py-2 text-[8px] text-[var(--v555-muted)]"} onClick={() => setMode(item)}>{item}</button>)}</div></div><p className="mt-3 text-[10px] leading-6 text-[var(--v555-muted)]">分类、redshift、image order 与 EVPA 均来自 v312/v313 CPU authority。Science 使用固定线性编码；Cinematic 只能改变面板表现，不能修改 payload 或 RGBA。</p></header>{!artifact ? <div className="mt-5 text-[10px] text-[var(--v555-muted)]">{state.status === "unavailable" ? `Sparse Science Image 不可用：${state.reason ?? "request-failed"}` : "正在读取 sparse authority…"}</div> : <><div className="mt-5 grid gap-2 sm:grid-cols-3 xl:grid-cols-6"><Metric label="samples" value="16" /><Metric label="capture" value="6" /><Metric label="escape" value="6" /><Metric label="disk hit" value="4" /><Metric label="display pixels" value={String(artifact.counts.sparseDisplayPixelCount)} /><Metric label="measured pixels" value="0" unavailable /></div><div className="mt-5 grid gap-2 sm:grid-cols-2 xl:grid-cols-4">{artifact.payload.samples.filter((sample) => sample.classification === "disk-hit").map((sample) => <article key={sample.rayId} className="rounded-2xl border border-white/10 bg-[var(--v555-raised)] p-4"><div className="flex justify-between gap-3"><span className="text-[9px] text-white">{sample.rayId}</span><span className="text-[8px] text-[var(--v555-qualified)]">disk-hit</span></div><div className="mt-3"><Row label="redshift" value={String(sample.redshift.value)} /><Row label="image order" value={String(sample.imageOrder.value)} /><Row label="EVPA" value={`${sample.evpa.valueDeg}°`} /><Row label="max residual" value={sample.maxNormalizedResidual.toExponential(2)} /></div></article>)}</div><footer className="mt-5 flex flex-wrap items-center justify-between gap-3 border-t border-white/10 pt-4 text-[8px] text-[var(--v555-muted)]"><span>sparse qualified · dense 0/49 · observed frame unavailable · formal v263</span><div className="flex gap-2"><Download id="csv" label="CSV" /><Download id="rgba" label="RGBA" /><Download id="atlas" label="JSON" /></div></footer></>}</section>; }
function Metric({ label, value, unavailable = false }: { label: string; value: string; unavailable?: boolean }) { return <div className="border border-white/10 bg-black/20 p-3"><div className="text-[8px] uppercase tracking-[.16em] text-[var(--v555-muted)]">{label}</div><div className={unavailable ? "mt-2 text-base text-[var(--v555-unavailable)]" : "mt-2 text-lg text-[var(--v555-qualified)]"}>{value}</div></div>; }
function Row({ label, value }: { label: string; value: string }) { return <div className="flex justify-between gap-3 border-b border-white/5 py-2 text-[8px]"><span className="text-[var(--v555-muted)]">{label}</span><span>{value}</span></div>; }
function Download({ id, label }: { id: "atlas" | "csv" | "rgba"; label: string }) { return <a className="border border-white/15 px-3 py-2 text-white/70" href={`/api/atlas/relativity-evidence/v555/sparse-science-image?download=${id}`}>{label}</a>; }
