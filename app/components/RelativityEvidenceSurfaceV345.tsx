"use client";
import { useEffect, useState } from "react"; import { parseRelativityEvidenceResponseV345, type RelativityEvidenceResponseV345 } from "../lib/relativityWorkbenchEvidenceV345";
export default function RelativityEvidenceSurfaceV345() { const [response, setResponse] = useState<RelativityEvidenceResponseV345>({ version: "v345-relativity-evidence-response", available: false, reason: "evidence-unavailable", snapshot: null }); useEffect(() => { const controller = new AbortController(); void fetch("/api/atlas/relativity-evidence/v345", { cache: "no-store", signal: controller.signal }).then(async (value) => parseRelativityEvidenceResponseV345(await value.json())).then(setResponse).catch(() => { if (!controller.signal.aborted) setResponse({ version: "v345-relativity-evidence-response", available: false, reason: "evidence-corrupt", snapshot: null }); }); return () => controller.abort(); }, []); const runtime = response.snapshot?.current.v345; const ready = response.available && runtime != null; return <section className="relative mt-2 overflow-hidden rounded-[10px] border border-sky-100/15 bg-[linear-gradient(145deg,rgba(4,15,25,0.96),rgba(5,9,17,0.98)_66%,rgba(10,18,29,0.96))] p-3 font-mono text-[8px] text-white/52" data-atlas-relativity-evidence-v345 data-atlas-relativity-evidence-v345-status={ready ? "ready" : response.reason}><div aria-hidden="true" className="pointer-events-none absolute inset-0 bg-[radial-gradient(circle_at_94%_0%,rgba(95,190,235,0.1),transparent_30%),linear-gradient(90deg,transparent_49.8%,rgba(148,220,255,0.025)_50%,transparent_50.2%)]" /><div className="relative flex flex-wrap items-end justify-between gap-2"><div><div className="text-[7px] uppercase tracking-[0.2em] text-sky-100/42">Runtime observatory v345</div><div className="mt-0.5 text-[13px] tracking-[0.045em] text-sky-50/90">Route / orientation / baseline replay</div></div><div className="text-right text-[7px] text-sky-100/64">{runtime?.denseStatus ?? "dense unavailable"}<br /><span className="text-white/28">non-react DOM telemetry</span></div></div><div className="relative mt-3 grid gap-1.5 sm:grid-cols-4"><div className="rounded border border-white/[0.08] bg-black/20 px-2 py-2"><div className="text-[7px] uppercase text-white/30">sequence</div><div className="mt-1 text-[12px] text-sky-50/84">{runtime?.sequence.length ?? 0}</div><div className="text-[7px] text-white/32">bounded phases</div></div><div className="rounded border border-white/[0.08] bg-black/20 px-2 py-2"><div className="text-[7px] uppercase text-white/30">orientation</div><div className="mt-1 text-[12px] text-sky-50/84">{runtime?.portraitAccepted && runtime.landscapeAccepted ? "P ↔ L" : "—"}</div><div className="text-[7px] text-white/32">no URL duplication</div></div><div className="rounded border border-white/[0.08] bg-black/20 px-2 py-2"><div className="text-[7px] uppercase text-white/30">runtime state</div><div className="mt-1 text-[12px] text-sky-50/84">Δ{runtime?.sceneRevisionDelta ?? 0} / {runtime?.canvasCount ?? 1}</div><div className="text-[7px] text-white/32">sceneRevision / Canvas</div></div><div className="rounded border border-white/[0.08] bg-black/20 px-2 py-2"><div className="text-[7px] uppercase text-white/30">final</div><div className="mt-1 text-[12px] text-emerald-50/84">{runtime?.finalBaseline ? "BASELINE" : "—"}</div><div className="text-[7px] text-white/32">PNG / FITS / leave</div></div></div><div className="relative mt-2 flex flex-wrap items-center justify-between gap-2 border-t border-white/[0.06] pt-2 text-[7px] text-white/32"><span>{runtime?.selectionStable ? "selection stable" : "selection pending"} · {runtime?.cameraLeaseBaseline ? "camera baseline" : "camera pending"} · {runtime?.noScienceMutation ? "Science unchanged" : "Science pending"}</span><span>{ready ? runtime.telemetryBridge : response.reason === "lite-boundary" ? "Lite boundary" : "evidence unavailable"}</span></div></section>; }
