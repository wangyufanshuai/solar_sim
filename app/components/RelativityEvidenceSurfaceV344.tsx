"use client";
import { useEffect, useState } from "react";
import { parseRelativityEvidenceResponseV344, type RelativityEvidenceResponseV344 } from "../lib/relativityWorkbenchEvidenceV344";
export default function RelativityEvidenceSurfaceV344() { const [response, setResponse] = useState<RelativityEvidenceResponseV344>({ version: "v344-relativity-evidence-response", available: false, reason: "evidence-unavailable", snapshot: null }); useEffect(() => { const controller = new AbortController(); void fetch("/api/atlas/relativity-evidence/v344", { cache: "no-store", signal: controller.signal }).then(async (value) => parseRelativityEvidenceResponseV344(await value.json())).then(setResponse).catch(() => { if (!controller.signal.aborted) setResponse({ version: "v344-relativity-evidence-response", available: false, reason: "evidence-corrupt", snapshot: null }); }); return () => controller.abort(); }, []); const lifecycle = response.snapshot?.current.v344; const ready = response.available && lifecycle != null; return <section className="relative mt-2 overflow-hidden rounded-[10px] border border-emerald-100/15 bg-[linear-gradient(145deg,rgba(4,20,18,0.94),rgba(5,10,18,0.98)_68%,rgba(8,18,28,0.96))] p-3 font-mono text-[8px] text-white/52" data-atlas-relativity-evidence-v344 data-atlas-relativity-evidence-v344-status={ready ? "ready" : response.reason}><div aria-hidden="true" className="pointer-events-none absolute inset-0 bg-[radial-gradient(circle_at_6%_0%,rgba(105,225,185,0.1),transparent_30%),linear-gradient(90deg,transparent_49.8%,rgba(148,220,255,0.025)_50%,transparent_50.2%)]" /><div className="relative flex flex-wrap items-end justify-between gap-2"><div><div className="text-[7px] uppercase tracking-[0.2em] text-emerald-100/44">Product lifecycle ledger v344</div><div className="mt-0.5 text-[13px] tracking-[0.045em] text-emerald-50/90">Intent / single-flight / release</div></div><div className="text-right text-[7px] text-emerald-100/66">{lifecycle?.denseStatus ?? "dense unavailable"}<br /><span className="text-white/28">browser qualification not run</span></div></div><div className="relative mt-3 grid gap-1.5 sm:grid-cols-4"><div className="rounded border border-white/[0.08] bg-black/20 px-2 py-2"><div className="text-[7px] uppercase text-white/30">before intent</div><div className="mt-1 text-[12px] text-emerald-50/84">{lifecycle?.beforeIntent.productFetchCount ?? 0}</div><div className="text-[7px] text-white/32">product fetches</div></div><div className="rounded border border-white/[0.08] bg-black/20 px-2 py-2"><div className="text-[7px] uppercase text-white/30">single-flight</div><div className="mt-1 text-[12px] text-emerald-50/84">{lifecycle?.concurrentPng.productFetchCount ?? 0} / {lifecycle?.concurrentPng.consumerCount ?? 0}</div><div className="text-[7px] text-white/32">fetch / consumers</div></div><div className="rounded border border-white/[0.08] bg-black/20 px-2 py-2"><div className="text-[7px] uppercase text-white/30">object URLs</div><div className="mt-1 text-[12px] text-emerald-50/84">{lifecycle?.concurrentPng.objectUrls ?? 0}</div><div className="text-[7px] text-white/32">per-consumer copies</div></div><div className="rounded border border-white/[0.08] bg-black/20 px-2 py-2"><div className="text-[7px] uppercase text-white/30">baseline</div><div className="mt-1 text-[12px] text-emerald-50/84">{lifecycle?.reverseBaseline && lifecycle.fitsBaseline && lifecycle.corruptBaseline ? "YES" : "—"}</div><div className="text-[7px] text-white/32">reverse / FITS / corrupt</div></div></div><div className="relative mt-2 flex flex-wrap items-center justify-between gap-2 border-t border-white/[0.06] pt-2 text-[7px] text-white/32"><span>{lifecycle?.exactShaVerified ? "exact SHA verified" : "SHA pending"} · {lifecycle?.idempotentRelease ? "idempotent release" : "release pending"} · {lifecycle?.mobilePreview ?? "no preview"}</span><span>{ready ? "telemetry summary" : response.reason === "lite-boundary" ? "Lite boundary" : "evidence unavailable"}</span></div></section>; }
