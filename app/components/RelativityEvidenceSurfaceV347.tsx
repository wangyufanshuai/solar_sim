"use client";
import { useEffect, useState } from "react"; import { parseRelativityEvidenceResponseV347, type RelativityEvidenceResponseV347 } from "../lib/relativityWorkbenchEvidenceV347";
export default function RelativityEvidenceSurfaceV347() { const [response, setResponse] = useState<RelativityEvidenceResponseV347>({ version: "v347-relativity-evidence-response", available: false, reason: "evidence-unavailable", snapshot: null }); useEffect(() => { const controller = new AbortController(); void fetch("/api/atlas/relativity-evidence/v347", { cache: "no-store", signal: controller.signal }).then(async (value) => parseRelativityEvidenceResponseV347(await value.json())).then(setResponse).catch(() => { if (!controller.signal.aborted) setResponse({ version: "v347-relativity-evidence-response", available: false, reason: "evidence-corrupt", snapshot: null }); }); return () => controller.abort(); }, []); const roi = response.snapshot?.current.v347; const ready = response.available && roi != null; return <section className="relative mt-2 overflow-hidden rounded-[10px] border border-violet-100/15 bg-[linear-gradient(145deg,rgba(20,10,38,0.95),rgba(5,9,17,0.98)_68%,rgba(10,17,29,0.96))] p-3 font-mono text-[8px] text-white/52" data-atlas-relativity-evidence-v347 data-atlas-relativity-evidence-v347-status={ready ? "ready" : response.reason}><div aria-hidden="true" className="pointer-events-none absolute inset-0 bg-[radial-gradient(circle_at_94%_0%,rgba(167,139,250,0.12),transparent_30%),linear-gradient(90deg,transparent_49.8%,rgba(148,220,255,0.025)_50%,transparent_50.2%)]" /><div className="relative flex flex-wrap items-end justify-between gap-2"><div><div className="text-[7px] uppercase tracking-[0.2em] text-violet-100/44">Scientific index ROI v347</div><div className="mt-0.5 text-[13px] tracking-[0.045em] text-violet-50/90">Radiance interval / provenance</div></div><div className="text-right text-[7px] text-violet-100/64">{roi?.denseStatus ?? "dense unavailable"}<br /><span className="text-white/28">browser qualification not run</span></div></div><div className="relative mt-3 grid gap-1.5 sm:grid-cols-4"><div className="rounded border border-white/[0.08] bg-black/20 px-2 py-2"><div className="text-[7px] uppercase text-white/30">ROIs</div><div className="mt-1 text-[12px] text-violet-50/84">{roi?.roiCount ?? 0}</div><div className="text-[7px] text-white/32">single-band contiguous</div></div><div className="rounded border border-white/[0.08] bg-black/20 px-2 py-2"><div className="text-[7px] uppercase text-white/30">summation</div><div className="mt-1 text-[12px] text-violet-50/84">NEUMAIER</div><div className="text-[7px] text-white/32">float64 compensated</div></div><div className="rounded border border-white/[0.08] bg-black/20 px-2 py-2"><div className="text-[7px] uppercase text-white/30">exports</div><div className="mt-1 text-[12px] text-violet-50/84">{roi?.jsonBytes ?? 0} / {roi?.csvBytes ?? 0}</div><div className="text-[7px] text-white/32">JSON / CSV bytes</div></div><div className="rounded border border-white/[0.08] bg-black/20 px-2 py-2"><div className="text-[7px] uppercase text-white/30">aperture flux</div><div className="mt-1 text-[12px] text-amber-50/84">UNAVAILABLE</div><div className="text-[7px] text-white/32">no solid angle / WCS</div></div></div><div className="relative mt-2 flex flex-wrap items-center justify-between gap-2 border-t border-white/[0.06] pt-2 text-[7px] text-white/32"><span>{roi?.deterministic ? "deterministic" : "pending"} · {roi?.intervalConserved ? "linear interval conserved" : "interval pending"} · {roi?.exportBaseline ? "export baseline" : "export pending"}</span><span>{ready ? "no RSS · no flux claim" : response.reason === "lite-boundary" ? "Lite boundary" : "evidence unavailable"}</span></div></section>; }
